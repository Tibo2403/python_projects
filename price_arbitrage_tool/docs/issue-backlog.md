# Issue Backlog

- Add a Streamlit match-review interface.
- Add category-specific fee and return presets.
- Export rejected, approved, and manual-review matches separately.
- Add tests for approved-match CSV aliases and marketplace selection.
- Add GitHub Actions that run `pytest` on push and pull requests.
