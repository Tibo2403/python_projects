# Issue Backlog

- Add cited source links to the final draft.
- Add `--output markdown` and `--output docx`.
- Add a no-network mock mode for demos and tests.
- Add tests for graph routing with mocked model/search clients.
- Add GitHub Actions for import and static contract checks.
