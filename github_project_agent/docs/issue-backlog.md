# Issue Backlog

- Add `--dry-run` to inspect a repository and propose changes without writing files.
- Add tests for allowlisted command handling.
- Add a validation report file under `.agent-workspace`.
- Add GitHub Actions for `pytest` and package import checks.
- Improve PR body generation with validation output and changed-file summaries.
